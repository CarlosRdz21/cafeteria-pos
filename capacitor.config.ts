import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cafeteria.pos',
  appName: 'cafeteria-pos',
  webDir: 'dist/cafeteria-pos'
};

export default config;
